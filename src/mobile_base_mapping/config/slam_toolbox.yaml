# Copyright 2026 Antigravity Team.
#
# S4 Mapping Subsystem configuration using slam_toolbox async_slam_toolbox_node.
# Fuses S2 merged LaserScan (/scan) and S3 dynamic odometry TF (odom -> base_footprint).
# Publishes /map (0.05m resolution OccupancyGrid) and acts as the sole authority for map -> odom TF in Mapping Mode.

async_slam_toolbox_node:
  ros__parameters:
    # Plugin solver
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Operating mode and coordinate frames
    mode: "mapping"
    map_frame: "map"
    odom_frame: "odom"
    base_frame: "base_footprint"
    scan_topic: "/scan"

    # Grid resolution and LiDAR limits (SYS-001)
    resolution: 0.05
    max_laser_range: 20.0
    min_laser_range: 0.05
    minimum_time_interval: 0.2
    transform_publish_period: 0.05 # 20 Hz map -> odom TF publication
    map_update_interval: 2.0

    # Scan matching and loop closure (SYS-006)
    use_scan_matching: true
    use_scan_barycenter: true
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Scan buffering and thresholds
    minimum_travel_distance: 0.2
    minimum_travel_heading: 0.2
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 20.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    do_search_space_decay: true
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
